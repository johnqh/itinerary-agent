node_modules/
dist/
build/
.next/
coverage/
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
*.log

.trueforge/
sandbox-scratch/

# A bare `data` pattern in a developer's global gitignore silently swallowed
# src/data/, which left the seed dataset untracked and the branch unbuildable.
# The seed dataset is a committed artifact of this project, not local scratch.
!src/data/
!src/data/**
